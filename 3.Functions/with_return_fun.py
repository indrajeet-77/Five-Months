def marks(m1:int,m2:int)->int:#here int is the data type which is returned as int
    total=m1+m2
    return total

print(marks(50,60))
#if we are using return then use print outside ,vise versa, and if we use both print inside the function as well as return then it will print output and NONE datatype



    
